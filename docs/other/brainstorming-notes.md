# Brainstorming Notes

This file holds exploratory ideas that are not yet important enough to become a plan, spec, or ADR.

## Candidate Future Ideas

- add a theme toggle to the greeting page
- add a language toggle between `Hello, world!` and another greeting
- add a small test note showing how a future implementation could be verified

## Why This File Exists

`docs/other/` is the pressure-release valve for ideas that should not clutter the active routing path.

If one of these ideas becomes actionable, move it into `docs/plans/` or `docs/specs/`.
